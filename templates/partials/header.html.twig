<header>
    <div class="form-row">
        <div class="col-md-4">
            <div class="picture-header">
                <a href="https://www.louvre.fr/">
                    <img style="" src="https://upload.wikimedia.org/wikipedia/fr/9/9f/Musee_du_Louvre_1992_logo.png" class="louvre-logo" alt="Logo image">
                </a>
            </div>
        </div>
        <div class="col-md-8">
            <div class="jumbotronbis text-center">
                <h1 class="display-5">Bienvenue sur la billetterie du musée du Louvre</h1>
                <hr>
                <P class "lead">Le musée du Louvre vous accueille tous les jours, sauf les mardis, les dimanches et jours fériés,<br>
                de 9h à 18h, jusqu'à 21h45 les jeudis et vendredis.<br>
                La fermeture des salles débute à 17h30 et à 21h30 les jeudis et vendredis.</p>
            </div>
        </div>
</header>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="https://www.louvre.fr/">Accueil</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('homepage') }}">Billetterie du musée du Louvre<span class="sr-only">(current)</span>
                </a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown show">
                <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="false" aria-expanded="false">Langue</a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                    <a class="dropdown-item" href="#">Français</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="#">Anglais</a>
                </div>
            </li>
        </ul>
    </div>
</nav>
<ol class="breadcrumb">
    <li class="breadcrumb-item">
        <a href="{{ path('homepage') }}">Choix des billets</a>
    </li>
    {% if reservation.id is defined and reservation.id is not null %}
        <li class="breadcrumb-item">
            <a href="#">Identification des visiteurs</a>
        </li>
    {% endif %}
    {% if (reservation.id is defined and reservation.id is not null) and  (reservation.clients.count == reservation.nbTicket) %}
        <li class="breadcrumb-item">
            <a href="#">Résumé de la commande</a>
        </li>
    {% endif %}
     {% if (reservation.id is defined and reservation.id is not null) and  (reservation.clients.count == reservation.nbTicket) and (reservation.isPaid == 1) %}
        <li class="breadcrumb-item">
            <a href="#">Confirmation de votre commande</a>
        </li>
    {% endif %}
</ol>